import log from "loglevel";

// Set the log level (e.g., 'trace', 'debug', 'info', 'warn', 'error')
log.setLevel("info");
log.setLevel("debug");
log.setLevel("trace");
log.setLevel("warn");
log.setLevel("error");

export default log;
